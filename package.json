{
  "name": "margin-attendance",
  "version": "1.0.0",
  "private": true,
  "description": "Margin: attendance tracker for college students (Android + desktop)",
  "engines": { "node": ">=22" },
  "scripts": {
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:icons": "npx @capacitor/assets generate --android",
    "android:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@tauri-apps/cli": "^2.0.0"
  }
}
